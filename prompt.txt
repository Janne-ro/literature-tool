I want to program a literature tool to organize my literature with a list of features. In the following i will describe the envisioned UI and each of the features 1. The Map: The left side and primary element of the UI should be a map of the literature with each node representing one paper. You should be able to zoom, pan and move nodes with drag and drop. The node should be named after the title of the paper and its color should be the first tag the paper was given. Inside the node visualize the publication year. They should be connected with edges that are defined for each node. 2. The sidebbar: When clicking on one of the nodes in the sidebar information should be visualized in the following order: Title, Authors (in academic formatting ie. X & Y for two and X et al. for more than 2), Year, Link, Article Type, Venue, Tags, Direct Quotes, Other Info. These fields should be editable with changes saved persistently. Also they should each use a markdown formatting. I should be able to close it to have the map almost full screen and open it again. The sidebar should also hold a button that lets me switch between showing the title of the paper as the title of the node or the auhors. Additioanlly it should hold the legend for the color of the nodes (i.e. whats each of their first tag) and something that lets me filter the visualized nodes (i.e. Visaulize certain date range, or only papers with certain tags) 3. Backend: All papers should be saved in a csv with the follwing structure: ID,Link,Authors,Year,Title,Article type,Venue,Direct quotes,Other info,Bibtex citation,Related papers,Tags. The fields authors, direct quotes, other info, and tags are lists that are seperated by ;. The field related papers has the format: " ID_002 | Same methodology" (first element meaning to which different paper the edge should go and second element what should be visualized written on the edge). 4. Adding papers: There should be a button in the bottom right that allows me to add papers. Clicking this button should open a visually appealing dialogue field where I can input the info of a new paper. This paper should then be saved persistently in the backend csv. 5. The table: There should be the option to switch from the map to a table format of all the currently visualized papers (just create a table from the csv) in which i should be able to pan through the table. To switch from the map to the table there should be a tab like switch mecahnism at the top. For the ui please use dark mode. Choose whichever programing language will be most effective to realize the project. First make an elaborate plan to ensure that the program and file structure makes sense and a checklist of what to do. Always test yourself. Then write the code to realize the project.